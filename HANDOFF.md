# 交接：这个项目怎么一步步走到今天的

> 给**新会话**看的。读完这一份 + `AGENTS.md`，你应该能无缝接手，不需要翻聊天记录。
> 最后更新：2026-09-25（电脑端会话收尾：真机契约修复 + 重打包 + 试玩台）。

---

## 0. 一分钟版本

| | |
|---|---|
| **是什么** | RNA 主题的祖玛。双轨道：出球道自动冒碱基（mRNA），玩家往三消道发 tRNA 珠做互补配对；**连续配对段长度是 3 的倍数**才消除；配错不扣分，而是触发**可连锁的爆炸** |
| **两个产物** | ① 网页版游戏本体（`src/` + `tools/`，纯前端单文件）② **千星奇域移植**（`miliastra/`，整个游戏翻译成 Lua 客户端脚本） |
| **现在在哪** | 代码全写完，**本地验证全绿**（对拍 81339 值零差异 / Lua 侧 164 项 / 本体 542 项 / 客户端运行时试玩台跑通）。卡在"搬到千星沙箱编辑器里"—— 见 §6 |
| **下一步** | 把 §6 的四步试完（脚本映射）；动手前先看 §11：**下载链接发出去的 zuma.lua 曾经是会崩的旧版，已重打包** |
| **仓库** | https://github.com/ihamn/zuma |

---

## 1. 项目是什么

**玩法一句话**：把《祖玛》的"同色三消"改写成"序列翻译"。

- **出球道**（大球）= mRNA：自动冒珠，绳子模型推动（喂入 / 推动传递 / 空隙 / 插入顶开）
- **三消道**（小球）= tRNA：玩家发射，打中互补的球就"读出"它（贴上绑定小球）
- **消除规则**：连续被读出的段，长度必须是 **3 的倍数**才整段消除
- **配错**：不扣分，而是给那颗球记上"错配"标记；当它左右两颗都已读出**且状态相同**时，连它一起**炸掉 3 颗**

**唯一规格**：`DESIGN.md`（约 4200 行）。所有数值、规则、取舍都在里面，带 § 编号。改玩法先改它。

---

## 2. 时间线：每一步做了什么

### 阶段 0 · 立项与设计
- 从一份聊天记录里提炼出玩法概念 → 写成 `DESIGN.md`
- 读了参考实现 `alula/CircleShootApp` 的反编译源码，写了 `refs/circleshoot/NOTES.md`
  （绳子模型的逐行依据就在那，`src/chain.js` 顶部注释引用了它）

### 阶段 1 · 网页版本体（U1~U14）
按 `DESIGN.md` 的单元编号推进，全部落在 `src/`（14 个文件，3722 行）：

| 单元 | 做了什么 |
|---|---|
| U1 | 工程骨架 + 无头自检台 + 单文件打包（`zuma.html`） |
| U2 | 几何内核：弧长参数化骨架 + 法线偏移双轨 + 轨道校验器 |
| U3 | 珠串模型（绳子模型）—— `src/chain.js`，逐行依据原版 `CurveMgr::AdvanceBalls()` |
| U4 | 层级与遮挡（分层绘制顺序） |
| U5~U6 | 渲染基线 + 核糖体控制器（旋转瞄准 / 双珠待命） |
| U7 | 发射与扫掠碰撞（胶囊测试，再快也不穿透） |
| U8 | 互补配对 + 副轨吸附（0.30s 吸附飞行） |
| U9 | 阅读框 + 三联体消除（3n 消，级联到不动点） |
| U10 | 爆炸与连锁 |
| U11 | 生命与计分（队头撞洞 → 整串被吸走 + 扣 1 命） |
| U12 | 加球（插入顶开，交给绳模型自然完成） |

### 阶段 2 · 玩法返工（用户逐条反馈驱动）
这一段是**需求变化最密集**的时期，很多现在的规则是用户当场否掉旧方案后重做的：

| 用户说什么 | 改了什么 |
|---|---|
| 「直接来个七球模式，匹配相当于元素反应」 | 新增元素反应模式（`src/elements.js`，反应表抄自官方 WIKI） |
| 「不想要 RNA 融入」（指元素模式） | 元素模式里去掉碱基/密码子那套词汇与机制 |
| 「新手关没必要来个长长的轨道吧？」 | §62：轨道长度按**这一关自己的球数**反解，不再统一 1.9 圈 |
| 「甚至可能连轨道都不需要」 | §63：**静止练习关** —— 没有轨道、没有洞穴、不前进，只教配对 |
| 「第一关融入了三的倍数，不太对；1、3 关可以合并；第 4 关没讲具体啥时候爆炸」 | §64：新手关重排，hint 全部重写（一关只教一件事） |
| 「七球模式不要加右下角切换按钮，洞穴往后放放」 | §65：模式按钮改成两态，洞穴关后移 |

### 阶段 3 · 千星奇域移植（本阶段的关键转折）
1. **考证**：抓了官方 **219 篇**文档做离线快照 → `miliastra/docs/01``02``03``04`
   - 一开始以为要走"节点图 + 场景实体 + 基础运动器"，**卡在硬限制上**：单条路径最多 50 个路点、
     没有每帧执行节点（只有 33Hz 定时器）、拿不到"打中的是第几颗"
   - **转折点**：读到官方原话「**客户端脚本可以实现绝大多数 2D 玩法**」→ 主路线改成
     **"一张客户端控件画布 + 一段 Lua 脚本"**。这一下把上面那些限制**全部作废**了
2. **数学内核移植**（8 个模块）→ 再用 **JS↔Lua 逐值对拍**证明跟本体一致
3. **规则层移植**（`ribosome.lua` + `board.lua`）→ **12 局完整对局**逐帧逐球对拍
4. **表现层 / 输入层 / 宿主胶水**（`ui.lua` `input.lua` `game.lua`）+ **假宿主** `lua/host/mock.lua`
5. **关卡数据导出**（`levels.js` → `levels_data.lua`，不手抄）

### 阶段 4 · 电脑端落地（就是现在这段）
- 用户要在电脑上用千星沙箱 → 做了电脑端包（`miliastra/pc/`）
- 发现**网易云游戏的机器不让访问游戏本地目录** → 卡在"脚本映射建不起来"（见 §6）
- 顺手把整个项目 + DSH 会话迁移都做成了"一个链接下到一整个包"

---

## 3. 关键决策：为什么不是另一个方案

| 决策 | 为什么 | 否决了什么 |
|---|---|---|
| **主路线 = 客户端 Lua 脚本** | 官方明说可实现绝大多数 2D 玩法；本作就是 2D。弧长模型可以**原样保留** | ❌ 节点图+实体：路点上限 50、无每帧节点、拿不到命中序号、要重写一遍逻辑 |
| **球不是实体，是图片控件** | 全部状态在 Lua 表里，碰撞就是纯数学 | ❌ 每颗球一个实体 + 受击盒 + 运动器 |
| **离散槽位**（曾作为路线 B 的方案） | 只有路线 B 才需要；已降级为备用资产 | — |
| **移植期间不改 `src/`** | 本体有 546 项测试当护栏；改本体会让"对拍"失去基准 | — |
| **跨语言必须有逐值对拍** | 这类移植最容易"看着对、其实错"，而且**不报错** | ❌ 靠肉眼看/靠"跑起来没崩" |

---

## 4. 验证体系（四层，缺一不可）

    node miliastra/tools/run-all.mjs      # 一条命令跑完全部

| 层 | 命令 | 当前数字 |
|---|---|---|
| ① JS↔Lua 逐值对拍 | `node miliastra/tools/parity.mjs` | **308 条命令 / 5702 行 / 81339 个数值，零差异** |
| ② Lua 沙箱检查 | `node miliastra/tools/check-lua-sandbox.mjs` | 14 文件 / 2782 行，0 问题 |
| ③ Lua 侧测试（假宿主） | `node miliastra/tools/test-lua.mjs` | **164 项断言**（含打包产物端到端跑通一整关 + 8 项真机契约断言） |
| ④ 客户端运行时试玩台 | `node miliastra/tools/sim-play.mjs --level=6 --frames=260 --play` | 114 个控件建出来，整关推进（没装模拟器则跳过） |
| ⑤ 本体回归 | 9 个 `tools/test-*.mjs`（460 项）+ `smoke.mjs`（82 项） | **542 项全绿** |

**对拍工具怎么用**：`parity.mjs` 生成一份命令表，JS 侧（`tools/lib/parity-js.mjs`）和
Lua 侧（`lua/parity/run.lua`）各写一个解释器执行它 —— **场景只写一次**，不会两边漂。
失败了用 `tools/parity-diff.mjs` 定位**第一个分叉**。

⚠ 电脑端两个环境坑已经写进代码，不用再踩：Windows 上 `python3` 常是 Microsoft Store 的占位程序
（`lib/python.mjs` 绕开）、Lua 解释器路径原来写死在 `vendor/bin/lua`（`lib/lua-runner.mjs` 现在按
`ZUMA_LUA` > vendor > PATH > Windows 默认安装位置 找，并报版本）。见 §9。

---

## 5. 发现的本体问题（**没改 `src/`**，等本体侧决定）

1. ★★ **存在"合法但无解"的盘面**：全关卡巡检实测 **11 关里 6 关会被卡死**。
   形态是"链首那颗是 mark=2"（标记序列像 `211`/`221`），而剩余 run 长度又不是 3 的倍数。
   根因在 `run.js` 的爆炸规则本身：链首/链尾的 2「缺一侧真邻居 → 待定」，注释写明设计意图是
   **"等冒球把它顶出边界再判"**。可这条规则隐含假设"链子会一直冒球" ——
   **限球关卡预算发满后就不再冒球，假设失效**，于是那两颗 2 永远不爆、剩下的 1 又凑不满 3 颗。
2. ★ **`startWpFrac` 留下 28 秒空档**（t6 洞穴关实测）：整链平移到 0.72×轨道长之后，
   冒球口到队尾之间有 400+ 单位是空的，新冒的球要 ~1700 帧才追上队尾。
   玩家看到的是"链子先僵住 28 秒，然后突然开始爬"。
3. ★ `scene.js:737` 引用了 **已被删除的常量** `DESIGN.baseRepeat` → 条件恒假。
   当前行为恰好是想要的，但这是一条会误导后来人的悬空引用。
   ⚠ 注意它**仍然消耗一次随机抽样**，删的时候必须保留这次消耗，否则整条随机序列改变。
4. `settle()` 用全局 `DESIGN.winOnClear` 而非关卡字段，探针想关掉它只能改全局常量。

---

## 6. 现在卡在哪：千星沙箱的脚本映射

**症状**：在千星沙箱里建脚本映射时，报
`不允许访问 C:\Users\netease\AppData\LocalLow\miHoYo\原神\BeyondLocal\<UID>\Beyond_Local_Save_Level\<关卡ID>\external_lua_file`

**已知**：
- 官方 219 篇文档里**完全没有** `external_lua_file` 这个路径 → 是沙箱自己的实现
- 那台机器是**网易云游戏**的（用户名 `netease`）
- 资源管理器里 **`C:\Users\netease\AppData\LocalLow` 这一层就被拒绝**（不只是子目录）
- 推测：**反作弊驱动 / 云沙箱挡住了"非游戏进程"访问游戏目录** —— 游戏自己有权限，资源管理器没有

### ★ 2026-09-25 下午的更正：**不是权限问题，是那个对话框**

在 DSH 里直接试了一遍（DSH 就是跑在这台云电脑上的进程）：

```
Test-Path C:\Users\netease\AppData\LocalLow\miHoYo\原神\BeyondLocal\190800866\Beyond_Local_Save_Level\1073741825
→ 有（整条路径都在，包括关卡目录）
Get-ChildItem ...\1073741825\external_lua_file
→ 是个**空目录**，能读能列
Copy-Item miliastra\out\zuma.lua → 该目录
→ **写成功**（116,734 字节落盘，哈希与仓库一致）
```

**结论**：`external_lua_file` 是"每个关卡一个"的目录，**对我们这个进程完全可写**。
所以旧判断"云沙箱把目录锁死了"是错的 —— 报"不允许访问"的是**沙箱里那个"选择本地文件"的对话框**
（它大概被限制只能看自己的沙箱目录，不能遍历 `LocalLow`）。这也解释了为什么 `.gil` 里
关卡名叫 `zuma_存档`、目录是沙箱自己 8:02 建出来的。

**由此得到的绕法（已做成工具）**：

    node miliastra/tools/pc-install-scripts.mjs        # 把 hello.lua + zuma.lua 直接放进所有 external_lua_file 目录
    node miliastra/tools/pc-install-scripts.mjs --dry  # 只看会放哪儿

它扫 `BeyondLocal/*/Beyond_Local_Save_Level/*/external_lua_file`，把两个脚本放进去并打印哈希。
云电脑重启会清空这个目录 → 回来重跑这一条即可。
目前唯一还没验证的是：**编辑器那个对话框会不会把预先放进去的文件列出来**（要么列出来直接选，
要么还是只能走「新建客户端脚本」）。这两种都试一下就有结论。

**还没试的（下一步要试的，按顺序）**：
1. 文件已经放好了 → 进编辑器试「新建客户端脚本」（官方文档里的第二种做法：让沙箱自己建文件）
   - 能建出来 = 沙箱有权限，把内容灌进去即可（或直接用我们放好的那份）
   - 也报同样的错 = 那个对话框被限制，回到下面第 3 条
2. 试「新建脚本映射」时把 `…\external_lua_file` 这个**完整路径粘进对话框地址栏**（别一层层点）
3. 管理员 PowerShell 拿权（现在看多半用不上）：`takeown` + `icacls`
4. 找网易云游戏客服（付费用户）问三件事：机型能不能读游戏本地目录 / 有没有文件上传功能 / 能不能换机型

**已经准备好的**：`miliastra/pc/` 里有
- `manual.html` —— 电脑端操作手册（一页纸速查 + 6 步 + 出错对照表 + external_lua_file 专节）
- `hello.lua` —— **5 KB 的最小验证脚本**（先验管线，再上 114 KB 的正主）
- `zuma.lua` —— 打包好的正式脚本（14 模块）
- `zuma-pc.zip` —— 上面三样打成一包
- 桌面副本（`1_先上这个_hello.lua` / `2_正式脚本_zuma.lua` / `3_操作手册_双击打开.html`）

★ 2026-09-25 更新：这两个脚本现在都**对齐了真机运行时契约**（§10）——
只要映射建起来、脚本挂上客户端控件，**第一帧不会再崩**；而且失败时游戏日志里会有
`[zuma] …` 的 print（print 不需要控件，白屏也能看到死在哪一步）。
把日志里那几行 `[zuma]` 念出来，就能判断是"没进 OnInit"还是"挂载点不对"。

---

## 7. 下一步（按优先级）

1. **【最高】把 §6 那四步试完** —— 这是唯一挡着"能玩上"的事。
   先上 `hello.lua`（5 KB）验管线，再上 `zuma.lua`；两者都会打 `[zuma]` / `[hello]` 日志。
2. 通了之后：在编辑器里搭 M1/M2（4 个控件模板 + 脚本映射 + 脚本变量），实测帧率
3. 选关菜单 / 结算界面 / 模式按钮 / 音效（M3/M4）
4. 把 §5 那四条本体问题拿回本体侧决定
5. 七球模式（元素反应）是否移植 —— 用户说过"先不管"
6. 想更快看到效果：`node miliastra/tools/sim-play.mjs --play --json=…` + `sim-render.mjs`（§11 第 4 条）

---

## 8. 名词表（省得新会话猜）

| 词 | 意思 |
|---|---|
| 本体 / `src/` | 网页版游戏源码，移植期间**不许改** |
| 路线 A / Lua 路线 | 主路线：客户端 Lua 脚本 + 客户端控件当画面 |
| 路线 B / 槽位路线 | 备用：节点图 + 场景实体 + 离散槽位（已降级，资产保留） |
| 对拍 / parity | JS 与 Lua 跑同一份命令表，逐值比对 |
| 假宿主 / mock | `lua/host/mock.lua`：照官方 API **+ 真机契约**做的无头环境，让表现层能本地验 |
| 真机契约 | 真机探测出来的运行时行为（`Id` 大小写、只读字段、OnStart 才能建控件、EnableUpdate…），见 §10 |
| 试玩台 / sim | `tools/sim-play.mjs`：在**千星客户端 Lua 运行时模拟器**里真跑我们的脚本 |
| 静止练习关 | 没有轨道、不前进、无洞穴的教学关（§63） |
| 3n 消 | 连续读出的段长度为 3 的倍数才整段消除 |
| mark 2 / 灰球 | 配错的球，等左右同为已读时才连爆 3 颗 |
| 千星沙箱 | 原神千星奇域的 PC 端编辑器 |

---

## 9. 电脑端（Windows）工具链怎么落地（2026-09-25）

原来 `run-all` 在这台电脑上**全红**，但红的是环境不是代码：

| 症状 | 原因 | 现在的做法 |
|---|---|---|
| `没找到 Lua 解释器：vendor/bin/lua` | `vendor/` 不进仓库，Windows 上又没有 bash/gcc 跑 `build-lua.sh` | `lib/lua-runner.mjs` 按 `ZUMA_LUA` > `vendor/bin/lua[.exe]` > PATH(`lua5.3`/`lua53`/`lua`) > `%LOCALAPPDATA%\Programs\Lua\bin\lua.exe` 找，**并在 run-all 第一行报版本** |
| `pack-pc.mjs` 退出码 9009 | `python3` 是 Microsoft Store 的占位程序（跑起来什么都不干） | `lib/python.mjs` 按 `ZUMA_PYTHON` > `python3` > `python` > `py -3` 探一个真的 Python 3 |
| 对拍报 **5702 处不一致**，可两边打印出来的数字一模一样 | Windows 下 Lua 的 stdout 是文本模式（`\n` → `\r\n`），每行**最后一列**多了个看不见的 `\r` | `parity.mjs` / `parity-diff.mjs` 先归一化行尾 |

本机装的是 **Lua 5.4.6**（`winget install DEVCOM.Lua`，落在 `%LOCALAPPDATA%\Programs\Lua`）。
千星奇域是 **5.3**；5.4 跑出来的 81339 个对拍值**与 5.3 全一致**，但版本提醒会一直挂在
run-all 第一行 —— 想严格对齐就装一个 5.3，或 `set ZUMA_LUA=...`。
手机端不受影响（`vendor/bin/lua` 由 `build-lua.sh` 产出，优先级更高）。

---

## 10. 真机运行时契约：一次"本地全绿、真机第一帧就崩"的教训

2026-09-25 从真机探针（社区模拟器 `miliastra-beyond-simulator` 的
`client/lua-runtime/docs/observed-contract.md`）+ 官方《客户端控件 API 文档》核出四条硬限制，
它们让**已经打包发出去的那一版 `zuma.lua` 在真机上必崩**：

| # | 真机行为 | 原来错在哪 |
|---|---|---|
| 1 | 控件 ID 字段是 **`Id`**（大写），小写 `id` 读出来是 nil | `ui.last[c.id]` → `table index is nil`，**第一帧崩**，且因为"报错那行字"本身也是控件，屏幕全白无提示 |
| 2 | `visible`/`active`/`alive`/`prefabIndex` **只读**，写报 `cannot set X, no such field` | `setField(c,'visible',…)` 直接赋值 |
| 3 | `InstantiateClientUIControl` 在 **OnInit 阶段返回 nil**，`OnStart` 才建得出来 | 原来在 `OnInit` 里建控件池 → 一个都没建出来 |
| 4 | **`EnableUpdate` 前没有 `OnUpdate`** | 不打开 → 画面永远不动 |

另一条：`game` 的函数是**点号调用**，`game:GetUICanvasSize()` 真机报
`bad argument count … (0 expected, got 1)`。

**为什么这次拦住了**：假宿主不是"照文档写个大概"，而是**照真机把字段和生命周期封死** ——
字段白名单（表外读 nil、写报错）、`Id` 大小写、只读字段、`idle/init/enable/start/running/destroy`
阶段、`EnableUpdate` 默认关、点号调用检查。契约断言在 `lua/test/test_mock.lua`、
`test_diag.lua`、`test_bundle.lua` 里（共 8 条，都带 ★）。
**教训**：本地测试之所以先把 `c.id` 放过去，是因为假宿主"什么字段都收" —— 坏的不是测试写少了，
而是**被测环境比真机宽松**。

---

## 11. 这一轮收尾改了什么（电脑端会话，2026-09-25）

上一段电脑端会话在 2:17 改完源码后被打断（C 盘 `.dsh` 被清空，会话记录丢了，**产物没丢**）。
这一轮把最后一段做完：

1. **真机契约四条**落到 `lua/src/game.lua`、`ui.lua`、`pc/hello.lua`（源码改动主要是这三处）。
2. **假宿主照真机封死** + 8 条契约断言 → Lua 侧断言 133 → **164 项**。
3. **工具链在 Windows 上跑通**（§9），`run-all` 从"3 步失败"变成**全部通过**。
4. **本地试玩台**收进仓库：`tools/sim-play.mjs`（在客户端运行时里真跑）+ `tools/sim-render.mjs`
   （把控件树画成 PNG），并接进 `run-all`（没装模拟器就跳过）。
5. **重打包电脑端**：`pc/zuma-pc.zip` 里的 `zuma.lua` 之前是 2:04 那一份（**会崩的旧版**），
   现在是修复版；`DOWNLOADS.md` 里的字节数/sha256 已同步更新。

⚠ 试玩台的已知假象（**别当真机结论**）：模拟器跑在 Fengari 上，**整数是 32 位**
（`0xFFFFFFFF == -1`），`rng.lua` 的掩码收不回无符号数 → `step()` 取值域变 `[-0.5, 0.5)`，
`rng.pick` 约一半取到 nil → 画面上出现**白色空球**。实测越界 10101/20000 次。
真机是 Lua 5.3（64 位）不会这样，本地对拍用的原生 Lua 也不会。
`sim-play.mjs` 会自己探整数位宽并警告，`sim-render` 把白球画成带 `?` 的空心圆。

---

## 12. 第二轮：表现层补齐（2026-09-25 同一天，用户看截图发现的问题）

用户看了试玩截图问："**匹配机制哪去了？发射出口哪去了？**" —— 对照网页版才知道，
`ui.lua` 只画了球 + 弹药 + HUD 文字，下面这些**一个都没画**（而且不画也不报错）：

| 网页版（`src/render.js`） | 移植侧之前 | 现在 |
|---|---|---|
| `drawRibosome`：核糖体（发射口）+ 两颗待发球 + 瞄准线 | ❌ 只有个 `if ui.hud.rb` 的**死分支**（`buildHudSpecs` 从没造过 `rb` 控件） | ✅ 照原式补上（待发球在 ±0.7R/0.8R，瞄准线长 `R+110×scale`） |
| `drawPairLink(s)`：读出后两颗球之间的连线（正确=直棒 / 错配=折线"断掉的键"） | ❌ 只改了颜色，看不出来配上没有 | ✅ 直棒 + 错配两段折线 |
| `drawBeadLayer` 的 `beads.eliminate`：副轨上的**绑定小球** | ❌ 没画 | ✅ |
| `drawMergeLayer`：并入过程中的球 | ❌ 没画 | ✅ |
| `drawCave`：降解洞穴 | ❌ 没画 | ✅ 红圈+暗心+文字（图片控件没描边/渐变，属近似） |
| `drawTrackLayer`：轨道 | ❌ | 仍然不画 —— **这是设计**：编辑期摆静态图（`out/path-<id>.svg`，`docs/05` L109） |

新增 `lua/test/test_visuals.lua`（40 项）盯着"画出来了没"；`ui.lua` 顶部注释写明
"画什么照 `render.js` 搬、画不出来的（描边/渐变/alpha）为什么"。
控件数：`ballCount=96` 时 **413 个**（≈4.3 × ballCount，上限 1000/组）。

同一天还顺手堵了两个坑：

1. **`rng` 越界会变成 nil**：32 位整数环境里 `step()` 跑出 `[0,1)`，`rng.pick` 取到 `arr[0]`/`arr[n+1]`
   = nil → 碱基 nil → 球画成白色，**而且不报错**。现在 `rng.int` 取下标前把值折回 `[0,1)`：
   64 位（真机）下是**恒等变换**（对拍 81339 个值零差异为证），32 位环境下也只是颜色序列不对、不至于崩。
   新增 `lua/test/test_rng.lua`（7 项）。
2. **光标坐标系**：官方 `GetCursorUIPos()` / `CursorEventData:GetUIPos()` 是
   "**画布左下角为原点、y 向上**"。`input.lua` 的 `toBoard()` 一直是对的，
   是**试玩台机器人**把屏幕坐标直接塞进去，导致截图上瞄准线上下镜像 —— 工具错了，游戏没错。

**数字变化**：Lua 侧断言 164 → **211**（+40 表现层 +7 rng）；打包 92,899 → **102,689 字节**；
电脑端 zip 41,747 → **45,259 字节**（`DOWNLOADS.md` 已同步）。

---

## 13. 第三轮：美化（2026-09-25 同一天，用户说"想想怎么美化"）

用户先问了一句"**有千星奇域的文档在仓库里吗**" —— 顺手办了两件事：

- **文档**：仓库里只有我们自己的 5 篇考证（`docs/01``02``03``04``05`）；**官方原文 219 篇在本机**
  `miliastra/docs/official/`（1.06 MB），**故意不进仓库**（`.gitignore`：第三方内容，可由
  `node miliastra/tools/fetch-docs.mjs` 重建）。顺手核了上游：**219 = 219，0 新增 0 删除 0 改动，
  `客户端控件API文档 [mhtakr07vej4]` 逐字相同** → 快照没过期，我们照它写的契约不用改。
- **纠错**：我原先在 `ui.lua` 注释里写"图片控件没有 alpha" —— **错的**。`imageColor` / `fontColor` /
  `bgColor` 都是 ColorValue，`Color.FromRGBA(r,g,b,a)` 第 4 个参数就是 alpha。所以网页版那些
  `rgba(150,200,255,0.30)` 可以原样搬。这条错误判断让"瞄准线"一开始是实心蓝的。

三档一起做完（用户选的）：

| 档 | 做了什么 | 用到的官方能力 |
|---|---|---|
| 一 · 颜色/发光 | 瞄准线 30% 透明；球的状态光晕（配对=互补色 / 配错=红）；洞穴三层红晕；核糖体柔边；HUD 半透明底板；**诊断行置顶**（原来被提示文字压着） | `Color.FromRGBA` 的 alpha、`enableSoftEdge` + `softEdgeMode` + `softEdgeWidthX/Y` |
| 二 · 动画 | 刚读出的绑定小球"弹"一下、开火后坐、被消掉的绑定小球"炸开淡出"、洞穴红晕呼吸 | `localScaleX/Y`（Tweenable）+ 自己算的短命效果表 |
| 三 · 大件 | **轨道由 Lua 画**（双轨路面，一段段"棒"拼；也可 `track=0` 用编辑器静态图）+ **球面碱基字母**（叠文本框，不依赖美术素材）+ **开火冷却环**（`SetFillRadial360`） | `railPolys` 折线 + 旋转的图片控件、文本框、径向填充 |

**动效为什么不用 `game.Tween`**：脏检查缓存按"上次写进去的值"判断要不要再写，而 Tween 绕过缓存直接改字段，
两条路同时改同一字段会打架。所以动画**只碰 sync 不碰的字段**（`localScaleX/Y`、临时改的颜色透明度），
自己用一个短命效果表推进 `ui.fx`。`game.Tween` 仍留着给将来"编辑期摆好的动效"用。

**三个开关**（脚本变量）：`fancy` / `track` / `letters`（都能设 0）。理由：**每写一个新字段都是一次真机风险**
（真机上字段不存在就直接报错），哪条炸了就改变量关掉，不用重新打包逻辑。
控件数：`ballCount=96` 时 **740 个**（≈ 7.7 × ballCount，上限 1000/组）——`trackSegments` 调小或关开关可压。

**踩到的坑（已写进规矩）**：
- **不能用 PowerShell 批量改 `.md`**：`Get-Content -Raw` 在中文 Windows 上默认按 **GBK** 解码，
  读进来已是乱码，再写回 UTF-8 就是"双重乱码"。本次一条替换命令同时写坏了 `AGENTS.md` 与
  `miliastra/README.md`，靠 `git checkout` 回滚重做（教训写进 `AGENTS.md`）。
- 第一版洞穴红晕给太大（1.42/1.84/2.26 倍半径 + 高透明度），整块红布糊在洞上 → 收到 1.16/1.32/1.48 且降到 25%/15%/8%。
- **空的"结果"文本框**一直在正中央画 640×120 的半透明底板，把核糖体整个盖住 → 改成**没文字就连底板一起隐藏**。

**数字变化（第三轮结束）**：Lua 侧断言 211 → **239**（表现层 40 → 68）；打包 102,689 → **116,238 字节**；
电脑端 zip 45,259 → **49,892 字节**；沙箱检查 2980 → 3275 行。`DOWNLOADS.md` 已同步。

---

## 14. 第四轮：把"填 4 个模板索引"这件事消灭掉（2026-09-25 同一天）

用户看到"脚本变量得我自己填？额……"之后的反应很合理 —— 那一步是**最枯燥也最容易抄错**的：
官方《客户端控件和客户端脚本》§八说「编辑时，可以通过点击客户端控件查看对应的控件模板索引ID」，
也就是要人一个个点开控件、抄 4 个大数字（形如 `1073741xxx`）再填进变量表；抄错的后果是
"球建不出来"或"点击没反应"，而且报错不会告诉你抄错了。

**做法**：官方还有 `typeof(value)`（"返回运行时类型名称；用于识别宿主对象"）。于是脚本在 `OnStart`
里**自己问**一遍：从索引 1 试到 32，每个索引试着建一个控件、看 `typeof` 是什么，
是 `…Image…` 就是球、`…TextBox…` 就是 HUD、`…Cursor…` 就是点击区、`…Container…` 就是玩区；
**探针建完立刻 `DestroyClientUIControl` 销毁**，不占控件数。填了变量的以变量为准，
没填用自动认出来的，自动认不出才退回 1/2/3/4。开关：`autoPrefabs`（默认 1）。

- 日志会打一行 `[zuma] 用这套模板索引：球=… 弹药=… 文本=… 光标=… 玩区=…`，可核对
- 模板顺序乱、索引不是 1/2/3/4（比如 7/9/12/15）也能认出来（有测试）
- 新增 `lua/test/test_prefabs.lua`（17 项）：什么都不填能跑、乱序能认、填了以填的为准、`autoPrefabs=0` 回老行为

**顺带修了一个假宿主失真**：原来 `InstantiateClientUIControl` 对**不存在的模板索引**会硬造一个图片控件
（真机不会，真机是建不出来）。这个失真会让"自动认模板"在本地永远认不出错 —— 现在未知索引返回 nil，
与真机一致。这也是本项目反复出现的那个模式：**本地环境比真机宽松 → 真机才炸**。

**数字**：Lua 侧断言 239 → **256**；打包 116,238 → **119,450 字节**；电脑端 zip 49,892 → **53,475 字节**。
`DOWNLOADS.md` 已同步；游戏目录（`…\external_lua_file`）与桌面副本都刷新过。

---

## 15. ★ 第一次真机试玩：日志说话了（2026-09-25 10:10 那轮）

用户"试玩啥也没有"。**沙箱自己的运行日志**（`BeyondLocal\<UID>\Beyond_Debug_Log\*.gia`，
二进制但字符串可读）把整件事说清楚了：

```
[zuma] OnInit：画布 %dx%d                                  ← 这行的 %d 没格式化成功（见下）
[zuma] EnableUpdate(true) -> ok
[zuma] tryBoot：开始建控件
[zuma] 挂载点 = ClientUIContainerControl:1                  ← ★ 脚本挂上了（挂的是画布容器）
[zuma] 自动认模板：球=nil 文本=nil 光标=nil 容器=nil          ← ★★ 真正的原因
[zuma] 用这套模板索引：球=1 弹药=1 文本=2 光标=nil 玩区=nil
[zuma] 诊断框 = nil
[zuma] 没建容器节点模板 → 直接用画布的默认容器节点当玩区
[zuma] tryBoot 失败：没找到"光标检测区域"控件模板：请到 界面控件组库 → 客户端控件模板 → …
```

**结论（三好一坏）**：
1. ✅ **脚本真的跑起来了** —— 映射、挂载、容器可见全都没问题（用户当时也确认"可见性是开的"）
2. ✅ **挂载点 = 画布容器**（`ClientUIContainerControl:1`）→ 我们"没有容器模板就用画布默认容器节点"
   这条兜底路径**在真机上走通了**
3. ✅ **`EnableUpdate(true)` 成功** → 每帧回调这条命脉在真机上没问题
4. ❌ **索引 1~32 里没有任何客户端控件模板** —— 动态创建无从谈起，所以白屏。
   要的是「界面控件组库 → 客户端控件模板 → 添加客户端控件 → **存为模板**」；
   **在画布里摆一个控件**是不够的（实例没有模板索引，脚本也拿不到）。

**顺手修的两个自己的问题**：
- `say('画布 %dx%d', w, h)` 打成了字面量 `画布 %dx%d` —— 真机的 canvas 尺寸是浮点，
  `%d` 遇到非整数会抛错，pcall 一接住就只剩格式串，**等于把最想看的信息吞了**。
  现在：全部改成 `%s`，且 `say()` 在格式化失败时退回"格式串 + 参数值"（丑但有用）。
- 自动认模板一个都没认出来时，现在会**额外打印**"该怎么建"（存为模板那句）和
  **前 8 个索引的探针结果**（`nil（没有这个模板）` / `建出 ClientUIImageControl` / `报错 …`），
  下次一眼就能区分"没建模板"和"建了但建不出来"。

这也再次印证了那条规矩：**诊断信息必须走不依赖控件的通道**（print）。
这一轮屏幕上什么都没显示（连诊断行都建不出来，因为文本框模板也不存在），
唯一能救我们的就是日志。
---

## 16. ★ 白屏真正的根因：模板索引是**大数字**，我扫错了空间（2026-09-25 10:22 那轮）

用户第二次试玩（日志 `日志_2026/09/25_10:22:12_玩家_凝銘`）说"**我存为模板了啊？？**" —— 他是对的。

日志显示探针 1~8 **全是 nil**，而我的自动认模板**只扫了 1~32**。但用户其实早就把答案发给我了：
他之前发过一个裸数字 **`1073741846`** —— 那是他点开模板看到的**控件模板索引**，
而它和关卡目录 `1073741825` 一样是 **2^30 起的大数字**（同一个 ID 空间）。
我却按 1/2/3/4 的思路去扫，于是"明明存了模板"也报 nil。**信息在手上，是我想岔了。**

修法（`game.lua` 的 `detectPrefabs`）：扫**两个索引空间** —— `1~32` 和 `2^30+1 ~ 2^30+192`；
小范围扫完（否则会漏掉常排在最后的容器模板），大范围只要三个必需的认出来就早退。
同时加了**回归测试**：`test_prefabs.lua` 里注册 `1073741846/1073741850/1073741860` 三个模板，
断言能认出来 —— 就是为了这条坑不再犯。

另外这轮日志还确认了两件事：
- `OnInit：画布 1815.0 x 899.99981689453` —— **真机画布尺寸是浮点**，
  这也解释了上一轮为什么 `%d` 抛错、只剩字面量 `%dx%d`（已改 `%s` + 失败回退打值）。
- `[UITree] Roots: 0` —— `game.PrintClientUITree()` 说运行时**一个 UI 根控件都没有**。
  按官方定义（`GetClientUIRoots()` = "实际显示的客户端控件容器画布中的默认容器节点"），
  0 = **画布在关卡运行时没生效/没显示**。所以脚本现在会在启动时打
  `客户端控件根控件数 = N`，0 就顺手提示去查【界面控件组管理 → 界面布局 → 初始可见】。
  ⚠ 这条**还没闭合**：模板建对之后再试玩，要看这一行是不是 0。
---

## 17. ★ 又一个"逐项枚举"的坑：`Enum.KeyEventType` 没有通用的 KeyDown（2026-09-25 10:27 那轮）

模板索引修好之后（`自动认模板：球=1073741852 文本=1073741851 光标=1073741850` ✅），
第三次试玩死在**最后一根钉子上**：

```
[zuma] tryBoot 失败：bad argument #2 to 'AddKeyEventListener' (KeyEventType expected, got nil)
```

**原因**：`input.lua` 里写的是 `Enum.KeyEventType.KeyDown` / `KeyUp`（通用名），
而真机（官方文档 164 个成员）是**逐键**命名：

```
KeyboardNormalAttackKeyDown / KeyboardNormalAttackKeyUp      （鼠标左键：开火）
KeyboardOpenShortcutWheelKeyDown                            （Tab：切模式）
KeyboardJumpKeyDown                                         （空格：换球）
KeyboardCharacterSkill3KeyDown                              （R：重开）
ControllerNormalAttackKeyDown / ControllerJumpKeyDown …     （手柄对应键）
```

而且**回调没有参数**（不是"给你一个键码自己比"）—— 注册哪个键就代表哪个键，返回 true = 已处理。
原来按"通用 KeyDown + 比较键码"写，两层都错。

**修法**（`input.lua`）：把逻辑动作映射到**候选事件名**，逐个探测 `Enum.KeyEventType[name] ~= nil`，
取第一个存在的；注册用 pcall；缺哪个就在日志里点名（`⚠ 按键事件 xxx 在这台机器上不存在`），
**不再让整局崩在注册那一行**。绑定结果也会打一行 `按键绑定：fireDown=… modeDown=…` 方便核对。

假宿主也照真机改了：`Enum.KeyEventType` 只提供逐键成员（**故意不提供 `KeyDown`**，
并在 `test_mock.lua` 里断言 `Enum.KeyEventType.KeyDown == nil`），`host.keyEvent()` 改成按
事件类型名派发、回调不带参数 —— 这样"按通用名写"这种错**本地就会炸**。

**顺手加的一条保险**：启动时把挂载点（客户端控件容器）的尺寸设成画布尺寸
（只设尺寸、**不动位置** —— 位置由布局决定，乱设会让整盘偏移；第一版连位置一起设，
把假宿主"根在画布中心"的约定打破，8 项断言当场红）。容器若在编辑器里是 0×0，
子控件会被裁掉，表现同样是"脚本跑通、屏幕空白"。

**这一课的通用教训**（和 §16 是一对）：千星奇域的枚举／ID 常常是**逐项**的
（逐键、逐类型、2^30 起的大数字），**没有**我们习惯的那种通用成员。
凡是看到"官方文档里那个枚举有 160 多个成员"的，就该警觉：不要假设 `Down/Up`、不要假设从 1 开始。
---

## 18. 真机第一次看到画面：以及一次"假警报"的教训（2026-09-25 10:38 那轮）

第四次试玩日志：**`tryBoot：成功`** —— 真机上第一次完整跑通：
模板自动认到（球=1073741852 文本=1073741851 光标=1073741850）、五个按键绑定成功、
96 个球的池子建出来、`关卡 t1-pair 已装配`。用户回："**有画面但是效果不行**"。

我当时的判断过程（值得留档，因为中间出了个假警报）：

1. 我按"真实画布比例 1815×900"用试玩台自己渲染了一张 → 看到游戏只占右半边、球偏小；
2. 又用 `tools/preview.mjs` 渲染了**网页版**在同尺寸下的样子 → 网页版的轨道明显大得多
   （我量到 L=2802 vs 移植版 1531，**差 55%**）→ 我判定"移植有 bug"；
3. **然后发现是假警报**：网页版 `LEVELS` 只导出 **4 个核心关**（spiral-outer 起），
   7 个新手关在 `TUTORIALS` 里；我拿"网页版 spiral-outer"比了"移植版 t1-pair"—— 两关在比。
   对齐同一关之后：
   | 画布 | 网页版 spiral-outer | 移植版 spiral-outer |
   |---|---|---|
   | 900×900 | 2354 | **2353.8** ✓ |
   | 1815×900 | 2802 | **2801.9** ✓ |
   **逐位吻合，移植是忠实的。**

**真实原因（两条，都不是 bug）**：
1. **默认 `levelIndex=1` 是静止练习关**（§63：无轨道运动、12 颗静止球）→ 第一印象当然"空"。
   已在 `manual.html` 第 5 步写明：第一次试玩请填 `levelIndex=8`（spiral-outer，大螺旋 + 48 球）。
2. **画布 1815×900 是 2:1**，而游戏按 `base = min(w,h)` 适配 → 按 900 高的方形算尺寸，
   横向留 40% 空白、球也偏小。手册里建议把客户端控件画布改成方形（1080×1080：
   球直径 38→46px、轨道半径 405→486，几乎铺满）。

**教训**：跨"移植版 vs 本体"对比时，**先把两侧的被测对象对齐**（关卡 id、画布尺寸、模式），
再谈数值差异 —— 否则很容易把"两关了"误判成"移植错了"，白折腾一轮（还差点去改没错的代码）。
---

## 19. ★ 规矩：屏幕上**默认不许出现本体没有的东西**（2026-09-25 用户："移植啊！不要乱改哦"）

用户这句话是对的，而且戳中了我这几轮的一个真问题：**为了远程排错，我往屏幕上加了不少本体没有的
文字**（左下角状态行、手里该打谁、上次命中判定）。排错期它们救了命，但它们**不属于移植**。

现在立成规矩并已改代码：

| 屏幕上的东西 | 本体有没有 | 移植版默认 |
|---|---|---|
| 分数 / 命 / 连读 / 模式 / 关卡提示 / 结果框 | 有 | **显示**（一一对应） |
| 左下角状态行（帧/关卡/球数/控件数/状态） | **没有** | **不显示** —— `diag=1` 才开 |
| 左上角教学两行（手里该打谁 / 上次命中判定） | **没有** | **不显示** —— `teach=1` 才开 |
| 出错信息 | 没有（本体不会崩） | **永远显示**（这是救命的那行，不受开关控制） |

三条自查原则（以后加任何东西前先问）：
1. **本体有吗？** 没有 → 默认关，且必须能用脚本变量打开。
2. **改变了玩法/数值吗？** 改了就是错的。判据不是"我看着像"，而是
   `node miliastra/tools/parity.mjs` **零差异**（现在：308 条命令 / 5702 行 / **81339 个数值完全一致**）。
3. **能关掉吗？** 表现层的每一处"我自己的发挥"（背板 / 光晕 / 球面字母 / 轨道自画）都要留开关，
   用户随时能退回"本体原样"。

**为什么这条规矩值得写下来**：移植期的排错需求和"忠实移植"是**冲突**的 ——
排错要往屏幕上堆信息，忠实要求屏幕上一个字都不多。解法不是二选一，而是**默认忠实、开关排错**。
---

## 20. 光标常驻：官方文档里那句"顺带"的话（2026-09-25）

用户问："现在还要按住 Alt 才能维持鼠标光标，能否实现常驻？"

答案在官方 API 文档 §24 `ClientUIContainerControl` 的字段表里，而且那句话还顺带解释了一个更严重的问题：

```
showCursor   boolean   读写   是否显示常驻光标；
                              ★ CursorEvent 相关方法都需设置该参数为真后才可正常使用
```

- **光标常驻** = `showCursor = true`（编辑器等价开关：容器节点控件 → 功能设置 → 【显示常驻光标】）
- **更要紧的是后半句**：`CursorEvent` 那一套方法（我们注册点击就是用它）**必须先把它设为真**。
  也就是说这很可能也是之前"点击/瞄准不灵"的一部分原因。

脚本现在在启动时自动打开（挂载点 `ClientUIContainerControl:1` 本身就是容器节点，不用额外建模板）：
```
[zuma] ★ 已开"显示常驻光标" showCursor=true（…）—— 不用再按住 Alt
```

**找它花了多久**：我最早在"容器节点控件"那篇文档里看到"可以使光标常驻显示"，再去 API 文档
的字段表里找具体字段名，才看到 showCursor。教训：**官方文档里那种"顺带一句"的功能描述，
往往就是某个 API 字段的入口**；看到"可以/支持/需先"这类词，就该去 API 文档里对字段名。